package pl.catalyst.model.product.converter;


import pl.catalyst.model.product.Product;
import pl.catalyst.utils.converter.JsonConverter;

import java.util.List;

public class ProductJsonConverter extends JsonConverter<List<Product>> {
    public ProductJsonConverter() {
    }
}
